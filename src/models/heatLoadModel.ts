// Re-export from grow-core — single source of truth (MeadowMakerAi/grow-core).
// The science lives in grow-core; this shim keeps the app' ../models/heatLoadModel imports stable.
export * from "grow-core/models/heatLoadModel";
