// Re-export from grow-core — single source of truth (MeadowMakerAi/grow-core).
// The science lives in grow-core; this shim keeps the app' ../models/vpdModel imports stable.
export * from "grow-core/models/vpdModel";
